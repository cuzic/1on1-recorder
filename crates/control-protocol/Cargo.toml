[package]
name = "control-protocol"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Wire types and local-socket naming shared by the desktop app's control server (control_server.rs) and the 1on1ctl CLI (apps/desktop-cli) — depends on neither."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
dirs = "6"
interprocess = "2"
