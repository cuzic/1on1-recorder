[package]
name = "capture-api"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "OS-independent state machine for safely rebinding an audio capture stream (microphone, endpoint loopback, or process loopback) across device changes, default-device switches, and process restarts."
readme = "README.md"
keywords = ["audio", "capture", "device", "state-machine"]
categories = ["multimedia::audio"]
